#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_concurrently@5.1.0@concurrently/bin/concurrently.js" "$@"
  ret=$?
else 
  node  "$basedir/../_concurrently@5.1.0@concurrently/bin/concurrently.js" "$@"
  ret=$?
fi
exit $ret
